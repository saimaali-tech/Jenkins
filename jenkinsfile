pipeline {
    agent any

    stages {
        stage('Build docker image') {
            steps {
              
                sh 'docker build -t my-sever-app .'
                sh 'docker images'
                sh 'docker ps'
            }
        }
        
    }
}