pipeline {
    agent any

    stages {
        stage('Build docker image') {
            steps {
              
                sh 'docker build -t my-sever-app .'
                sh 'docker run -dp 3000:3000 my-server-app'
                sh 'docker ps'
            }
        }
        
    }
}